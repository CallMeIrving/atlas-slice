# AtlasSlice 图集切片 — 需求文档

> 版本：v1.0（定稿）
> 日期：2026-09-21
> 状态：已确认，待开发

## 1. 项目概述

一个面向游戏开发/美术人员的**图集切片工具**：将游戏图集（Sprite Atlas / Spritesheet）按元数据、透明边缘或手动框选方式切分为独立小图，并支持批量导出、动画预览与配置预设。

### 1.1 技术形态

- **纯本地 Web 工具**，所有图片处理均在浏览器内完成，**不上传服务器**，素材零泄露风险
- 技术栈：Vue 3 + TypeScript + Vite
- 构建后为静态页面，可本地运行或任意静态托管

### 1.2 约束

- 单个图集文件大小限制 **10MB 以内**，超出时给出明确提示（不做超大图分块处理）
- 无需后端服务、无需数据库、无需第三方图像处理服务

## 2. 功能需求

### 2.1 输入

| 功能 | 说明 |
|---|---|
| 图集图片导入 | 支持 PNG / JPG / WebP，支持拖拽或点击选择 |
| 元数据导入 | 与图集图片配套导入，格式自动识别 |
| 文件夹/批量导入 | 一次导入多个图集，支持批量处理 |

#### 2.1.1 支持的元数据格式

- **TexturePacker JSON**：哈希表结构与数组结构两种均支持
- **plist**：Cocos2d-x / SpriteKit 常用格式
- **XML**：通用 XML 元数据
- 格式自动识别，无需手动指定

### 2.2 裁切方式（三种并存）

1. **读取元数据裁切**：按 atlas 数据中的 frame rect、rotated、trimmed 信息精确裁切
2. **自动识别透明边缘**：逐像素扫描透明边界，自动生成裁切框
3. **手动框选**：画布上拖拽矩形框选，支持微调、删除、重命名

### 2.3 手动框选精度四件套

| 能力 | 说明 |
|---|---|
| **边缘磁吸（Snapping）** | 拖拽时自动吸附到最近的透明↔不透明内容像素边界 |
| **智能贴合（Auto-fit）** | 一键将当前框收缩到框内不透明像素的最小包围盒 |
| **透明区域遮罩** | 框内透明区域用半透明遮罩实时高亮，直观显示框入的透明区域 |
| **像素级微调** | 键盘方向键单边移动 1px（Shift+方向键 10px）+ 局部放大镜精确对准像素 |

补充：
- 内容判定使用**不透明度阈值**（如 `alpha > 10` 算内容像素），阈值可配置，可过滤半透明噪点
- 框四边可分别拖动，无需整体缩放

### 2.4 核心处理能力

| 功能 | 说明 |
|---|---|
| 透明像素裁切 | 自动去除透明边，得到"实际内容尺寸" |
| 保留原始帧尺寸 | 保留原始 frame 尺寸（含透明区域） |
| 统一留边 padding | 裁切后可统一补充 N 像素透明留边，防止合图出血 |
| 旋转处理 | 正确处理元数据中 `rotated: true` 的子图 |
| 批量处理 | 一次导入多个图集，批量完成裁切 |

#### 2.4.1 尺寸模式（可选）

- **实际内容尺寸**：裁掉透明边后的最小包围盒尺寸
- **原始帧尺寸**：保留原始 frame 尺寸（含透明区域）

### 2.5 导出

| 功能 | 说明 |
|---|---|
| 独立 PNG | 每个子图导出为独立 PNG，保留透明通道 |
| 打包 ZIP | 批量导出结果打包为一个 ZIP 供整体下载（浏览器端生成，无需后端） |
| 同步导出元数据 | 导出附带一份新 JSON，记录裁剪信息、位置、尺寸 |
| 命名规则 | 自定义前缀 / 后缀 / 序号格式 |
| 目录结构 | 支持按子目录分组导出 |

### 2.6 动画预览

- 按帧序播放，支持播放速度调节
- 单帧暂停 / 步进
- 循环开关

#### 2.6.1 帧排序规则

- **默认：自然数字排序**（1、2、10 而非 1、10、2）
- 可选模式：元数据顺序、按命名规则/正则提取序号
- 排序结果可**手动拖拽调整**，调整后的顺序随预设保存

### 2.7 配置预设

- 将**留边值、尺寸模式、命名规则、输出配置、动画播放参数**打包为一个预设
- 可保存多套预设，存储于 **localStorage**（刷新不丢）
- 界面提供预设选择器，一键套用

### 2.8 其他

- 处理进度反馈（进度条）
- 结果核对：每个子图可放大检查，可重新裁切
- 框选结果的偏差提示（实际内容尺寸 vs 框选尺寸）

## 3. 非功能需求

- **性能**：10MB 以内文件流畅处理，像素扫描不阻塞 UI（必要时用 Web Worker）
- **兼容性**：主流现代浏览器（Chrome / Edge / Safari / Firefox）
- **易用性**：支持拖拽导入，界面简洁，暗色主题可选
- **数据安全**：所有数据本地处理，不产生任何网络请求上传素材

## 4. 技术方案（参考）

| 类别 | 方案 | 用途 |
|---|---|---|
| 运行环境 | Node.js + npm/pnpm | 开发环境 |
| 前端框架 | Vue 3 + TypeScript + Vite | 界面与工程化 |
| 图片处理 | 原生 Canvas 2D + `createImageBitmap` / `OffscreenCanvas` | 绘制、截取、像素读取 |
| 文件读取 | File API + `FileReader` / `URL.createObjectURL` | 本地文件加载 |
| 元数据解析 | JSON 原生 `JSON.parse`；plist/XML 用轻量库（如 `fast-xml-parser`） | 解析 atlas 数据 |
| ZIP 打包 | `jszip` | 浏览器端打包下载 |
| 像素扫描 | `getImageData` + Web Worker（可选） | 透明边缘检测、框选辅助 |
| 预设存储 | `localStorage` | 配置保存 |

## 5. 待定 / 后续可扩展

- 反向合图功能（小图合成图集）——本期不做，可后续扩展
- 超大图集分块处理——本期通过 10MB 限制规避，如未来放开限制需引入 tile 分块方案
- 导出格式扩展（WebP 等）——本期仅 PNG
- 多语言切换——本期仅中文

## 6. 里程碑建议

1. **M1 基础骨架**：Vue3 工程搭建、页面布局、文件导入（图片 + 元数据）
2. **M2 元数据裁切**：JSON / plist / XML 解析、精确裁切、独立 PNG 导出
3. **M3 手动框选**：画布交互、框选精度四件套、透明边缘自动识别
4. **M4 导出与批量**：ZIP 打包、命名规则与目录结构、批量处理
5. **M5 预览与预设**：动画预览、帧排序、配置预设、进度反馈
