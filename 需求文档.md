# AtlasSlice 图集切片 — 需求文档

> 版本：v2.3（功能盘点整理稿）
> 日期：2026-09-24
> 说明：本文档按实际开发状态整理。✅ 已开发；⚠️ 部分实现；❌ 未开发。本期不计划开发的功能已从正文移除，汇总见「范围裁剪决策」。

## 1. 项目概述

一个面向游戏开发/美术人员的**本地动画素材处理工具**：将游戏图集、视频和独立图片转换为可用于动画的标准帧，并支持自动识别、尺寸统一、本地抠图、批量处理、动画预览和多种导出方式。

### 1.1 技术形态

- **纯本地 Web 工具**：图片、视频处理均在浏览器内完成，**不上传业务素材**
- 技术栈：Vue 3 + TypeScript + Vite
- 构建后为静态页面，可本地运行或任意静态托管

### 1.2 约束

- 单个图集文件大小限制 **10MB 以内**，超出时给出明确提示
- 无需后端服务、无需数据库、无需第三方图像处理服务
- 页面之间切换时保留各页面当前状态，不丢失未导出的编辑内容

## 2. 范围裁剪决策

### 2.1 已确认移除（历史决策）

以下能力确认不纳入本项目版本范围：

1. 自然语言提示词抠图
2. GIF 导出
3. 音频节奏分析抽帧
4. 帧间运动补偿和自动去抖
5. 实时摄像头抠图
6. 反向合图
7. 多语言切换
8. 复杂的模型管理后台、模型版本升级和自动校验服务——当前采用浏览器缓存、本地模型目录和手动预加载，满足单机使用需求

### 2.2 本期功能精简（本次新增移除）

在不改变三页核心定位的前提下，删除成本高、收益低的功能点：

| 页面 | 移除的功能 | 原优先级 | 移除理由 |
|---|---|---|---|
| 精灵图 | 批量导入多图集 / 批处理 | P0 | 页面已冻结，单图集流程已闭环 |
| 抠图 | AI 模型推理（ISNet / RMBG） | P0 | 已反转：AI 推理已接入（见 4.2），不再移除 |
| 抠图 | 手动修正（画笔/橡皮擦） | P1 | 依赖蒙版编辑，交互成本高 |
| 抠图 | 边缘平滑 / 羽化 / 去噪 / 透明反转 | P0 | 依赖蒙版编辑能力，随手动修正一并延后 |
| 抠图 | 统一画布尺寸 / 居中 / 底部对齐 / X-Y 偏移 | P0 | 为批量规范化设计，本期无批量需求 |
| 抠图 | 蒙版 PNG / 原图对照图导出、文件名前缀/后缀/序号 | P0 | 导出增强，暂缓 |
| 抠图 | 画布缩放 / 平移 / 适应 / 1:1 | P0 | 已完成，支持滚轮缩放、按钮缩放、适应画布和拖动平移 |
| 视频帧 | 自定义时间轴 + 抽取位置标记 | P0 | 原生播放器控件已满足基础预览 |
| 视频帧 | 单帧延迟 / 标记处理状态 / 单帧重新生成 | P0 | 帧级管理增强，暂缓 |
| 视频帧 | 批量抠图任务：进度 / 暂停 / 取消 / 失败重试 | P0 | 需任务队列 + Worker 体系 |
| 视频帧 | 统一处理：居中 / 底部对齐 / X-Y 偏移 / 最大内容画布 / 统一透明边距 / 统一延迟 | P0 | 批量规范化，暂缓 |
| 视频帧 | 雪碧图导出 / JSON 帧信息导出 | P0 | 导出增强，暂缓 |

## 3. 页面状态总览

| 页面 | 状态 | 说明 |
|---|---|---|
| 精灵图 | ✅ 已完成并冻结 | 功能已满足需求，后续不再新增 |
| 抠图 | ✅ 基础版可用 | 颜色/自动/纯色抠图、AI 模型、模型预加载和画布操作已接入 |
| 视频帧 | 🚧 基础版可用 | 抽帧/预览/批量抠图已可用；任务管理未做 |

## 4. 功能清单与开发状态

### 4.1 精灵图页（✅ 已完成，冻结）

| 功能 | 状态 |
|---|---|
| 元数据导入：JSON / plist / XML，格式自动识别 | ✅ |
| 元数据裁切（rect / rotated / trimmed） | ✅ |
| 自动识别透明边缘 | ✅ |
| 手动框选 + 透明区域遮罩 | ✅ |
| 框选精度：边缘磁吸 / 智能贴合 / 像素级微调（方向键） | ✅ |
| 尺寸模式：实际内容 / 原始帧尺寸 | ✅ |
| 统一留边 padding | ✅ |
| 旋转处理 | ✅ |
| 导出：独立 PNG / 打包 ZIP / 元数据（JSON / plist） | ✅ |
| 命名规则（前缀 / 后缀 / 序号）与目录结构 | ✅ |
| 动画预览：播放速度 / 暂停 / 步进 / 循环 | ✅ |
| 帧排序：自然数字排序 / 拖拽调整 | ✅ |
| 配置预设（localStorage 保存与套用） | ✅ |

### 4.2 抠图页（🚧 基础版可用）

| 功能 | 状态 |
|---|---|
| 自动抠图（基于背景/边缘颜色） | ✅ |
| 颜色抠图（颜色采样 + 容差） | ✅ |
| 纯色背景抠图（自动采样边缘主色） | ✅ |
| 软阈值羽化 + 色溢抑制（边缘防残留） | ✅ |
| 自动裁切透明边缘 | ✅ |
| 预览背景：棋盘格 / 白底 / 黑底 / 原图 | ✅ |
| 导出透明 PNG | ✅ |
| 导出 PNG + 配置 JSON（ZIP 打包） | ✅ |
| AI 模型推理（imgly ISNet / RMBG，下拉切换 + 参数面板） | ✅ 已接入 |
| AI 模型预加载与实例复用 | ✅ |
| 模型状态确认：未加载 / 加载中 / 已加载 / 加载失败 | ✅ |
| HuggingFace 官方源与 `hf-mirror.com` 镜像切换 | ✅ |
| 本地模型目录加载（`public/models/`） | ✅ |
| 处理方式帮助弹窗、使用场景和模型推荐 | ✅ |
| 大图首次加载按工作区完整适配并居中 | ✅ |
| 画布滚轮缩放、缩放按钮、适应画布 | ✅ |
| 画布拖动平移 | ✅ |
| 手动修正（画笔/橡皮擦） | ❌ 未开发（已精简） |
| 边缘平滑 / 羽化 / 去噪 / 透明反转 | ❌ 未开发（已精简） |

### 4.3 视频帧页（🚧 基础版可用）

| 功能 | 状态 |
|---|---|
| 抽帧：按数量 / 按帧率 / 时间区间 | ✅ |
| 帧列表：勾选 / 全选 / 删除 / 拖拽排序 / 序号+时间戳 | ✅ |
| 动画预览：播放 / 暂停 / 速度 / 循环 / 前后单帧 | ✅ |
| 翻转（左右）与旋转（0/90/180/270） | ✅ |
| 批量移除背景（纯色背景抠图 + 容差，复用颜色抠图算法） | ✅ |
| 导出：当前帧 PNG / 勾选帧 ZIP | ✅ |
| 批量抠图任务：进度 / 暂停 / 取消 / 失败重试 | ❌ 未开发（已精简） |
| 自定义时间轴 + 抽取位置标记 | ❌ 未开发（已精简） |
| 雪碧图导出 / JSON 帧信息导出 | ❌ 未开发（已精简） |

### 4.4 模型资源与离线使用

| 功能 | 状态 |
|---|---|
| 模型下载地址和镜像地址文档 | ✅ |
| 浏览器缓存复用 | ✅ |
| 点击「预加载当前模型」提前下载并初始化 | ✅ |
| 页面刷新后自动预加载当前 AI 模型 | ✅ |
| 已初始化模型实例复用，避免重复加载 | ✅ |
| 模型加载状态和失败原因展示 | ✅ |
| `public/models/` 本地模型目录约定及 RMBG 示例资源 | ✅ |
| RMBG 严格使用本地模型，缺失文件时阻止远程回退 | ✅ |
| 本地模型文件逐项检查（HTTP 状态、内容类型和文件大小日志） | ✅ |
| 开发服务器对不存在的 `/models/` 文件返回 404，避免 HTML 回退为模型 JSON | ✅ |
| 抠图模型配置变更自动保存到工作区设置 | ✅ |
| Transformers.js 图像输出与分割蒙版输出兼容处理 | ✅ |
| 模型后台管理、自动更新和版本校验 | ❌ 未开发 |

## 5. 非功能需求

- **性能**：10MB 以内图片流畅处理，像素扫描和耗时处理不阻塞 UI
- **兼容性**：主流现代浏览器（Chrome / Edge / Safari / Firefox），视频格式以浏览器实际解码能力为准
- **数据安全**：所有素材本地处理，不上传图片、视频和帧数据
- **易用性**：支持拖拽导入，界面简洁，暗色主题
- **最低页面宽度**：1440px，小于该宽度保持横向工作区
- **可观测性**：耗时操作显示加载状态和错误原因

## 6. 验收标准（按当前范围）

- 精灵图：元数据裁切 / 自动识别 / 手动框选 / 导出结果准确（✅ 已通过）
- 抠图：颜色/自动抠图生成透明 PNG，边缘无明显残留色（✅ 基础版）
- 视频帧：抽帧数量准确；预览只播放勾选帧；批量抠图生效（✅ 基础版）
- 导出：ZIP 文件数与导出范围一致（✅）
- 画布：大图首次加载完整居中，支持缩放、适应画布和拖动平移（✅）
- 模型：可预加载当前模型，重复处理复用实例，并显示加载状态和错误原因（✅）
- 测试资源：使用 `public/assets/testImages/` 下的资源完成回归验证（✅）

## 7. 测试资源与回归约定

### 7.1 统一资源目录

后续功能测试优先从 `public/assets/testImages/` 获取素材。当前目录包含：

- `红底人物证件照 .png`：红色纯色背景人像，验证纯色抠图、边缘处理和 AI 人像模型
- `gameAssets.png`、`gameAssets2.png`：图集切片、透明边缘识别和导出
- `gameUI.png`：游戏 UI 图集和手动框选
- `ChatGPT Image 2026年9月23日 18_43_47.png`：通用图片测试

如果现有资源无法覆盖测试场景，应自行生成最小测试资源，例如纯色背景图、带透明边缘的图集、超大尺寸图片或多目标图片，并将可复用资源放入该目录。临时生成文件不应提交到仓库，除非它属于后续回归测试资产。

### 7.2 本次回归结果

- 使用 `红底人物证件照 .png` 完成导入验证。
- 纯色背景抠图成功，自动识别基准色 `#9b030c`。
- 验证 AI 模型状态从「未加载」进入预加载入口；刷新页面后会自动重新初始化当前模型。
- 验证本地模型目录按当前精度加载，缺失文件和 HTML 错误页会被明确识别并报错。
- 验证模型配置变更会自动保存，重复预加载会复用已初始化实例。
- 使用大尺寸合成图验证首次完整适配、居中和画布拖动。
- `pnpm run build` 通过，`git diff --check` 通过。

## 8. 里程碑（更新）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1–M6 | 工程骨架 → 精灵图全功能 | ✅ 完成 |
| M7 精简 | 抠图基础版（颜色/自动抠图） | ✅ 完成 |
| M8 | 视频抽帧 + 预览 | ✅ 完成 |
| M9 精简 | 视频批量抠图（帧级内联处理） | ✅ 完成 |
| M10 | 联调验收、性能与兼容性收尾 | 🚧 进行中 |
| M11 | 抠图 AI 模型接入、预加载和画布体验完善 | ✅ 完成 |
| M12 | 本地模型资源接入、离线加载保护和模型状态持久化 | ✅ 完成 |
