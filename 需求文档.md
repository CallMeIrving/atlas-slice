# AtlasSlice 图集切片 — 需求文档

> 版本：v2.0（图集 + 抠图 + 视频帧需求整理稿）
> 日期：2026-09-21
> 状态：待新任务确认后开发

## 范围裁剪决策

以下能力已确认砍掉，不纳入本项目当前版本及后续开发范围：

1. 自然语言提示词抠图
2. GIF 导出
3. 音频节奏分析抽帧
4. 帧间运动补偿和自动去抖
5. 实时摄像头抠图
6. 反向合图
7. 多语言切换
8. BiRefNet、MediaPipe、WASM、WebGPU 多套模型并行支持
9. 复杂的模型缓存和离线模型部署

当前版本只保留基础颜色抠图 / 基础自动抠图，不接入多模型推理体系。

## 页面范围冻结

精灵图页面当前功能已满足需求，本项目后续不再对精灵图页面新增功能、扩展格式或进行非必要重构。后续开发仅聚焦抠图页面和视频帧页面。

## 1. 项目概述

一个面向游戏开发/美术人员的**本地动画素材处理工具**：将游戏图集、视频和独立图片转换为可用于动画的标准帧，并支持自动识别、尺寸统一、本地 AI 抠图、批量处理、动画预览和多种导出方式。

### 1.1 技术形态

- **纯本地 Web 工具**，图片、视频和模型推理均在浏览器内完成，**不上传业务素材**
- 技术栈：Vue 3 + TypeScript + Vite
- 构建后为静态页面，可本地运行或任意静态托管

### 1.2 约束

- 单个图集文件大小限制 **10MB 以内**，超出时给出明确提示（不做超大图分块处理）
- 无需后端服务、无需数据库、无需第三方图像处理服务
- 本期不接入 AI 模型下载、模型缓存或离线模型部署

## 2. 功能需求

### 2.0 页面与导航

应用拆分为三个相互独立的功能页面，顶部使用统一导航栏：

| 页面 | 目标 | 当前范围 |
|---|---|---|
| **精灵图** | 图集切片、帧尺寸统一、动画预览和雪碧图导出 | 已完成并冻结 |
| **抠图** | 对单张图片进行自动抠图、交互式抠图和手动修正 | 本期新增 |
| **视频帧** | 从视频抽取动画帧、预览、批量抠图和导出 | 本期新增 |

页面要求：

- 页面之间切换时保留各页面当前状态，至少不影响未导出的编辑内容
- 导航栏显示当前页面和处理状态
- 视频帧页面第一版不依赖精灵图页面才能使用
- 抠图和视频帧页面共用模型选择、透明背景预览、批量任务和导出能力

### 2.1 输入

| 功能 | 说明 |
|---|---|
| 图集图片导入 | 支持 PNG / JPG / WebP，支持拖拽或点击选择 |
| 元数据导入 | 与图集图片配套导入，格式自动识别 |
| 文件夹/批量导入 | 一次导入多个图集，支持批量处理 |

#### 2.1.1 支持的元数据格式

- **TexturePacker JSON**：哈希表结构与数组结构两种均支持
- **plist**：Cocos2d-x / SpriteKit 常用格式
- **XML**：通用 XML 元数据
- 格式自动识别，无需手动指定

### 2.2 裁切方式（三种并存）

1. **读取元数据裁切**：按 atlas 数据中的 frame rect、rotated、trimmed 信息精确裁切
2. **自动识别透明边缘**：逐像素扫描透明边界，自动生成裁切框
3. **手动框选**：画布上拖拽矩形框选，支持微调、删除、重命名

### 2.3 手动框选精度四件套

| 能力 | 说明 |
|---|---|
| **边缘磁吸（Snapping）** | 拖拽时自动吸附到最近的透明↔不透明内容像素边界 |
| **智能贴合（Auto-fit）** | 一键将当前框收缩到框内不透明像素的最小包围盒 |
| **透明区域遮罩** | 框内透明区域用半透明遮罩实时高亮，直观显示框入的透明区域 |
| **像素级微调** | 键盘方向键单边移动 1px（Shift+方向键 10px）+ 局部放大镜精确对准像素 |

补充：
- 内容判定使用**不透明度阈值**（如 `alpha > 10` 算内容像素），阈值可配置，可过滤半透明噪点
- 框四边可分别拖动，无需整体缩放

### 2.4 核心处理能力

| 功能 | 说明 |
|---|---|
| 透明像素裁切 | 自动去除透明边，得到"实际内容尺寸" |
| 保留原始帧尺寸 | 保留原始 frame 尺寸（含透明区域） |
| 统一留边 padding | 裁切后可统一补充 N 像素透明留边，防止合图出血 |
| 旋转处理 | 正确处理元数据中 `rotated: true` 的子图 |
| 批量处理 | 一次导入多个图集，批量完成裁切 |

#### 2.4.1 尺寸模式（可选）

- **实际内容尺寸**：裁掉透明边后的最小包围盒尺寸
- **原始帧尺寸**：保留原始 frame 尺寸（含透明区域）

### 2.5 导出

| 功能 | 说明 |
|---|---|
| 独立 PNG | 每个子图导出为独立 PNG，保留透明通道 |
| 打包 ZIP | 批量导出结果打包为一个 ZIP 供整体下载（浏览器端生成，无需后端） |
| 同步导出元数据 | 导出附带一份新 JSON，记录裁剪信息、位置、尺寸 |
| 命名规则 | 自定义前缀 / 后缀 / 序号格式 |
| 目录结构 | 支持按子目录分组导出 |

### 2.6 动画预览

- 按帧序播放，支持播放速度调节
- 单帧暂停 / 步进
- 循环开关

#### 2.6.1 帧排序规则

- **默认：自然数字排序**（1、2、10 而非 1、10、2）
- 可选模式：元数据顺序、按命名规则/正则提取序号
- 排序结果可**手动拖拽调整**，调整后的顺序随预设保存

### 2.7 配置预设

- 将**留边值、尺寸模式、命名规则、输出配置、动画播放参数**打包为一个预设
- 可保存多套预设，存储于 **localStorage**（刷新不丢）
- 界面提供预设选择器，一键套用

### 2.8 其他

- 处理进度反馈（进度条）
- 结果核对：每个子图可放大检查，可重新裁切
- 框选结果的偏差提示（实际内容尺寸 vs 框选尺寸）

### 2.9 抠图页面

#### 2.9.1 输入与工作区

- 支持导入 PNG / JPG / WebP 图片
- 支持拖拽导入和点击选择
- 左侧为工具栏，中间为棋盘格画布，右侧为参数和模型面板
- 支持原图、透明结果、纯色背景和前后对比预览
- 支持缩放、平移、适应画布和 1:1 查看

#### 2.9.2 抠图方式

| 方式 | 说明 | 优先级 |
|---|---|---|
| 自动抠图 | 使用本地模型自动生成前景蒙版 | P0 |
| AI 抠图 | 通过模型选择器切换不同本地模型 | P0 |
| 自动框选 | 识别主体并生成候选框 | P0 |
| 手动框选 | 用户拖拽矩形区域限定处理范围 | P0 |
| 前景点击 | 点击需要保留的区域 | P0 |
| 背景点击 | 点击需要移除的区域 | P0 |
| 颜色抠图 | 使用颜色采样和容差移除相近颜色 | P0 |
| 画笔/橡皮擦 | 对蒙版进行局部增加和擦除 | P1 |

#### 2.9.3 本地模型方案

本期不接入 BiRefNet、MediaPipe、WASM、WebGPU 等模型推理方案。抠图仅提供基础颜色抠图和基于背景色的自动抠图。

#### 2.9.4 抠图调整

- 阈值调整
- 边缘平滑
- 羽化
- 去除小面积噪点
- 透明区域反转
- 自动裁剪透明边缘
- 统一画布尺寸
- 角色居中、底部对齐和手动 X/Y 偏移

#### 2.9.5 抠图导出

- 导出透明 PNG
- 导出蒙版 PNG
- 导出原图与抠图对照图
- 导出处理配置 JSON
- 文件名支持前缀、后缀和序号

### 2.10 视频帧页面

#### 2.10.1 视频导入

- 支持浏览器可解码的 MP4、WebM、MOV 等格式
- 支持拖拽导入和点击选择
- 导入后显示视频名称、时长、分辨率、原始帧率
- 浏览器无法解码时给出明确提示
- 全部处理在浏览器本地完成，不上传视频

#### 2.10.2 抽帧模式

提供抽帧模式下拉框，默认使用“按数量均匀抽取”：

1. **按数量抽取**
   - 输入目标帧数
   - 在指定时间区间内均匀采样
   - 适合制作固定帧数的角色动画
2. **按帧率抽取**
   - 输入目标 FPS，例如 8、12、24
   - 按时间区间生成帧
   - 适合保持固定动画节奏
3. **按时间区间抽取**
   - 设置开始时间和结束时间
   - 可结合按数量或按帧率使用

边界规则：

- 目标帧数必须为正整数
- 开始时间不能大于结束时间
- 结束时间不能超过视频时长
- 抽帧前显示预计帧数和预计内存占用
- 大量帧处理时使用队列和分批生成，避免一次性阻塞页面

#### 2.10.3 视频预览

- 原视频播放和暂停
- 时间轴拖动
- 当前时间显示
- 抽取位置标记
- 抽取帧数量显示
- 单帧预览
- 抽取结果网格预览
- 抽取结果动画预览
- 播放速度调节
- 循环播放
- 单帧前进、后退

动画预览必须只播放当前帧列表，不播放整个原视频。

#### 2.10.4 帧列表

每一帧支持：

- 勾选和取消勾选
- 批量全选、取消全选
- 删除帧
- 调整顺序
- 显示序号和原视频时间戳
- 设置单帧延迟时间
- 标记处理状态
- 对单帧重新生成
- 查看原图与处理结果

导出、批量抠图和动画预览都必须遵循当前帧列表的勾选范围，不能误处理全部帧。

#### 2.10.5 视频帧统一处理

抽帧后支持批量：

- 统一宽度和高度
- 按最大内容尺寸自动计算画布
- 保持宽高比
- 角色居中
- 底部对齐
- 手动调整 X/Y 偏移
- 统一透明边距
- 自动裁剪透明边缘
- 左右翻转、上下翻转和旋转
- 设置统一帧延迟

默认预览需显示统一处理后的动画效果，方便检查角色是否抖动、跳位或大小不一致。

### 2.11 视频帧批量抠图

视频帧页面提供“批量抠图”入口，支持：

- 处理全部帧
- 处理勾选帧
- 只处理当前帧
- 使用基础颜色 / 背景色抠图处理
- 显示总数、成功数、失败数和处理进度
- 暂停、继续和取消任务
- 失败帧重试
- 保留原始帧
- 批量应用统一边缘参数

批量任务结果至少包含：

```text
总帧数：40
已完成：18
成功：17
失败：1
当前状态：处理中 / 已完成 / 已取消
```

批量抠图完成后提供四种动画检查模式：

- 原始帧
- 抠图结果
- 棋盘格透明背景
- 原图与抠图对比

重点检查：角色位置抖动、尺寸变化、边缘闪烁、背景残留和局部缺失。

### 2.12 视频帧导出

导出下拉框支持：

- 导出全部帧
- 导出勾选帧
- 导出透明 PNG
- 导出 ZIP
- 导出雪碧图
- 导出 JSON 帧信息

导出范围必须与当前选择状态一致。导出雪碧图时复用现有雪碧图布局能力，支持紧凑排列、固定网格、列数、间距、外边距、透明背景和 JSON / plist 坐标。

JSON 帧信息至少包含：

```json
{
  "frame": 1,
  "sourceTime": 0.125,
  "width": 256,
  "height": 256,
  "delay": 100,
  "x": 0,
  "y": 0
}
```

## 3. 非功能需求

- **性能**：10MB 以内图片流畅处理，像素扫描和视频抽帧不阻塞 UI（必要时用 Web Worker）
- **兼容性**：主流现代浏览器（Chrome / Edge / Safari / Firefox），视频格式以浏览器实际解码能力为准
- **易用性**：支持拖拽导入，界面简洁，暗色主题可选
- **数据安全**：所有素材和推理结果本地处理，不上传图片、视频和帧数据；模型文件允许首次下载并缓存
- **最低页面宽度**：1440px；小于该宽度时保持横向工作区，不压缩核心操作区域
- **任务可恢复**：批处理任务支持暂停、取消和失败重试，不因单帧失败导致整体中断
- **可观测性**：所有耗时操作显示加载状态、进度、成功数和失败原因

## 4. 技术方案（参考）

| 类别 | 方案 | 用途 |
|---|---|---|
| 运行环境 | Node.js + npm/pnpm | 开发环境 |
| 前端框架 | Vue 3 + TypeScript + Vite | 界面与工程化 |
| 图片处理 | 原生 Canvas 2D + `createImageBitmap` / `OffscreenCanvas` | 绘制、截取、像素读取 |
| 文件读取 | File API + `FileReader` / `URL.createObjectURL` | 本地文件加载 |
| 元数据解析 | JSON 原生 `JSON.parse`；plist/XML 用轻量库（如 `fast-xml-parser`） | 解析 atlas 数据 |
| ZIP 打包 | `jszip` | 浏览器端打包下载 |
| 像素扫描 | `getImageData` + Web Worker（可选） | 透明边缘检测、框选辅助 |
| 预设存储 | `localStorage` | 配置保存 |

补充方案：

| 类别 | 方案 | 用途 |
|---|---|---|
| 页面导航 | 轻量路由或 Vue Router | 三个独立功能页 |
| 视频解码 | HTMLVideoElement + Canvas | 视频定位与抽帧 |
| 自动抠图 | Canvas 2D 背景色处理 | 本地基础自动抠图 |
| 批处理 | Worker 或分批异步队列 | 避免阻塞主线程 |

模型策略：

- 本期不包含模型选择、模型加载、推理后端切换和模型缓存能力

## 5. 待定 / 后续可扩展

- 反向合图功能（小图合成图集）——已砍掉
- 超大图集分块处理——本期通过 10MB 限制规避，如未来放开限制需引入 tile 分块方案
- 导出格式扩展（WebP 等）——本期仅 PNG
- 多语言切换——已砍掉
- 自然语言提示词抠图——已砍掉
- 视频背景移除和实时摄像头抠图——实时摄像头抠图已砍掉；基础背景色处理保留
- 音频节奏分析后自动抽帧——已砍掉
- 帧间运动补偿和自动去抖——已砍掉
- GIF 导出——已砍掉
- 复杂模型缓存和离线模型部署——已砍掉

## 6. 验收标准

### 页面与导航

- 可以在精灵图、抠图、视频帧三个页面之间切换
- 当前页面有明显状态标识
- 页面在 1440px 宽度下布局完整

### 视频抽帧

- 可以导入浏览器支持的视频
- 按数量抽取时，生成帧数与目标数量一致
- 按帧率抽取时，生成数量符合时间区间和 FPS 计算结果
- 设置时间区间后不会抽取区间外帧
- 动画预览只播放当前帧列表
- 删除或取消勾选帧后，预览和导出结果同步变化

### 抠图与批处理

- 基础自动抠图能生成透明 PNG
- 可以通过点选或框选修正主体
- 视频帧可以批量抠图并显示进度
- 单帧失败不会导致全部任务失败
- 批量处理后能统一尺寸和对齐

### 导出

- 导出全部和导出勾选的结果数量准确
- ZIP 中的文件数量和当前导出范围一致
- 雪碧图预览与最终导出布局一致
- JSON 中的帧序号、尺寸、位置和时间信息准确

## 7. 里程碑建议

1. **M1 基础骨架**：Vue3 工程搭建、页面布局、文件导入（图片 + 元数据）
2. **M2 元数据裁切**：JSON / plist / XML 解析、精确裁切、独立 PNG 导出
3. **M3 手动框选**：画布交互、框选精度四件套、透明边缘自动识别
4. **M4 导出与批量**：ZIP 打包、命名规则与目录结构、批量处理
5. **M5 预览与预设**：动画预览、帧排序、配置预设、进度反馈
6. **M6 页面拆分**：导航栏、精灵图页、抠图页、视频帧页
7. **M7 抠图能力**：模型选择、本地推理、自动抠图、点选/框选、颜色抠图
8. **M8 视频抽帧**：视频导入、按数量/帧率/时间范围抽帧、帧列表和动画预览
9. **M9 批量抠图**：视频帧批处理、进度管理、统一尺寸和对齐
10. **M10 联调验收**：批量导出、雪碧图、异常处理、性能和浏览器兼容性
